# CM-SHC on MIR-Flickr-25k @ 64 bits (bit-budget ablation).
model:
  name: cm_shc
  bit_dim: 64
  similarity_method: classifier

dataset: mirflickr25k

experiment_name: cmshc_mirflickr25k_64bit_clf

training:
  max_epochs: 200
  batch_size: 64
