# DCMH on MIR-Flickr-25k (merges base + dcmh model + mirflickr25k dataset + overrides below).
model: dcmh
dataset: mirflickr25k

experiment_name: dcmh_mirflickr25k_128bit

training:
  max_epochs: 50
  batch_size: 128
